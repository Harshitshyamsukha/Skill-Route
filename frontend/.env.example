# Copy to .env.local and fill in values. NEVER commit .env.local.

# Backend URL (leave empty when using the Vite proxy in development)
VITE_API_BASE_URL=

# API key — must match the API_SECRET_KEY in the backend .env
VITE_API_KEY=change_me_to_match_backend_API_SECRET_KEY
